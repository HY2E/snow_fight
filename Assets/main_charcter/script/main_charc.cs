using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class main_charc : MonoBehaviour
{
    public GameObject obmainCharc;
    // Start is called before the first frame update
    void Start()
    {
        obmainCharc.transform.position = new Vector3(0,0,0);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
